# 云梦高校科研竞赛管理系统 - 快速部署指南

## 🚀 一键部署

### Windows 环境

1. **启动数据库初始化**
   ```cmd
   cd go-backend
   init_database.bat
   ```

2. **启动后端服务**
   ```cmd
   start.bat
   ```

3. **启动前端服务**
   ```cmd
   cd ../yunmeng-frontend
   npm install
   npm run dev
   ```

### Linux/Mac 环境

1. **启动数据库初始化**
   ```bash
   cd go-backend
   chmod +x init_database.sh
   ./init_database.sh
   ```

2. **启动后端服务**
   ```bash
   chmod +x start.sh
   ./start.sh
   ```

3. **启动前端服务**
   ```bash
   cd ../yunmeng-frontend
   npm install
   npm run dev
   ```

## 📋 环境检查清单

### 必需软件
- [ ] Go 1.20+
- [ ] Node.js 16+
- [ ] MySQL 8.0+
- [ ] Git

### 数据库配置
- [ ] MySQL服务已启动
- [ ] 数据库 `cloud_dream_system` 已创建
- [ ] 数据库用户权限正确

### 网络配置
- [ ] 端口 8080 未被占用
- [ ] 端口 5173 未被占用
- [ ] 防火墙允许相应端口

## 🔧 故障排除

### 常见错误及解决方案

#### 1. 数据库连接失败
```
错误: 数据库连接失败
```
**解决步骤:**
1. 检查MySQL服务状态
2. 验证数据库连接参数
3. 确认数据库用户权限

#### 2. 数据库迁移错误
```
错误: Incorrect table definition
```
**解决步骤:**
1. 执行数据库初始化脚本
2. 检查现有表结构
3. 手动执行迁移脚本

#### 3. 端口被占用
```
错误: address already in use
```
**解决步骤:**
1. 修改端口配置
2. 停止占用端口的进程
3. 使用不同的端口

#### 4. 依赖安装失败
```
错误: go mod tidy 失败
```
**解决步骤:**
1. 检查网络连接
2. 配置Go代理
3. 清理模块缓存

## 🧪 测试验证

### 1. 数据库连接测试
```bash
cd go-backend
go run test_simple.go
```

### 2. 用户服务测试
```bash
go run test_user_service.go
```

### 3. API接口测试
```bash
# 测试登录接口
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456","role":"admin"}'
```

### 4. 前端访问测试
- 打开浏览器访问: http://localhost:5173
- 使用默认账号登录: admin / 123456

## 📊 系统状态检查

### 后端服务状态
- [ ] 服务启动成功
- [ ] 数据库连接正常
- [ ] API接口响应正常
- [ ] 日志输出正常

### 前端服务状态
- [ ] 开发服务器启动成功
- [ ] 页面加载正常
- [ ] 组件渲染正常
- [ ] API调用正常

### 数据库状态
- [ ] 表结构正确
- [ ] 初始数据完整
- [ ] 外键约束正常
- [ ] 索引创建成功

## 🔐 安全配置

### 生产环境建议
1. **修改默认密码**
   - 数据库root密码
   - 系统管理员密码
   - JWT密钥

2. **配置HTTPS**
   - 申请SSL证书
   - 配置反向代理
   - 启用HTTPS重定向

3. **防火墙配置**
   - 限制数据库端口访问
   - 配置应用端口规则
   - 启用入侵检测

4. **日志监控**
   - 配置日志轮转
   - 设置日志级别
   - 监控异常访问

## 📈 性能优化

### 数据库优化
1. **索引优化**
   - 为常用查询字段添加索引
   - 定期分析查询性能
   - 优化慢查询

2. **连接池配置**
   - 调整最大连接数
   - 配置连接超时
   - 监控连接使用情况

### 应用优化
1. **缓存策略**
   - 添加Redis缓存
   - 实现数据缓存
   - 配置缓存失效策略

2. **并发处理**
   - 优化数据库查询
   - 使用异步处理
   - 配置工作池

## 🔄 更新部署

### 代码更新
1. **拉取最新代码**
   ```bash
   git pull origin main
   ```

2. **更新依赖**
   ```bash
   # 后端
   cd go-backend
   go mod tidy
   
   # 前端
   cd ../yunmeng-frontend
   npm install
   ```

3. **重启服务**
   ```bash
   # 重启后端
   pkill -f "go run main.go"
   go run main.go
   
   # 重启前端
   pkill -f "npm run dev"
   npm run dev
   ```

### 数据库更新
1. **备份数据**
   ```bash
   mysqldump -u root -p cloud_dream_system > backup.sql
   ```

2. **执行迁移**
   ```bash
   mysql -u root -p < sql/migrate_existing.sql
   ```

3. **验证数据**
   ```bash
   go run test_simple.go
   ```

## 📞 技术支持

### 获取帮助
- **文档**: 查看项目README文件
- **问题反馈**: 提交GitHub Issue
- **邮件支持**: admin@yunmeng.edu.cn

### 日志收集
当遇到问题时，请收集以下信息：
1. 错误日志
2. 系统环境信息
3. 复现步骤
4. 期望结果

### 社区支持
- **GitHub**: 项目仓库
- **讨论区**: GitHub Discussions
- **Wiki**: 项目Wiki页面 